#include <bits/stdc++.h>

using namespace std;

void solve() {
    string s1, s2;
    cin >> s1 >> s2;
    cout << ((s1.size() < s2.size()) ? "no" : "go") << '\n';    
}
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    solve();
    return 0;
}

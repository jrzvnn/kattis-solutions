//Solving for Carrots
#include <stdio.h>

int main()
{
    int n, p, i;
    char s[100];
    
    scanf("%d %d", &n, &p);
    
    for (i = 0; i < n; i++)
    {
        scanf("%s", s);
    }
    
    printf("%d\n", p);

    return 0;
}

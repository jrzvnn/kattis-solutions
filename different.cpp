#include <bits/stdc++.h>
typedef long long ll;

using namespace std;

void solve() {
    ll a, b;
    while(cin >> a >> b) {
        cout << abs(a - b) << '\n';
    }
}
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    solve();
    return 0;
}

